<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coronacheckcustomer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MoveSchool"></activity>
        <activity android:name=".ChangeDetails" />
        <activity android:name=".TryActivity" />
        <activity android:name=".MainScreen" />
        <activity android:name=".Register" />
        <activity android:name=".JoinOrganiztion" />
        <activity android:name=".AddChildren" />
        <activity android:name=".ChangeProfile" />
        <activity android:name=".ChangePass" />
        <activity android:name=".CoronaDecleration" />
        <activity android:name=".BusinessRegister" />
        <activity android:name=".Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>